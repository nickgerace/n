# Agents

- For VCS, source control, etc. tasks, only use `jj` and do not use `git`
- You can use `gh` for GitHub-related work, but not `git` (only `jj`)
- Do not push or fetch commits, branches, tags, etc.
