# Manifests

- For required description fields in tofu and terraform files, write one concise and accurate sentence that says what the value is for, with no filler (e.g. "The AWS region to deploy into." rather than "This variable is used to specify the AWS region that resources will be deployed into.")
